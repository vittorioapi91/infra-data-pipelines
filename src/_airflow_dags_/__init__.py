"""
Airflow DAGs for TradingPythonAgent

This directory contains Airflow DAGs that are installed to .ops/.airflow/dags/
during the build process using scripts/install-dags.sh.

DAGs are located in src/_airflow_dags_/ to keep them within
the trading_agent package structure.
"""
